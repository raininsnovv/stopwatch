import React, { useState } from "react";

const Counter = ({count}) => {

  return (
    <div style={{ marginTop: "50px" }}>
      {" "}
      <h2>potatoes: {count}</h2>
    
    </div>
  );
};

export default Counter;
 